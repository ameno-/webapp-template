{
  "name": "webapp-template-workers",
  "version": "1.0.0",
  "description": "Cloudflare Workers API for webapp-template",
  "main": "api/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev",
    "db:migrate": "wrangler d1 execute webapp-template-db --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute webapp-template-db --local --file=./schema.sql"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "hono",
    "api"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "hono": "^4.3.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.9.3",
    "wrangler": "^3.57.0"
  }
}
